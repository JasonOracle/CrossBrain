# 2026-09-15 工作日志

> 承接 2026-09-14 深夜的会话（TASK-19 立项后立即开工，跨零点）。
> 详细交付清单与实测输出见 `docs/impl/TASK_BREAKDOWN.md` 的「TASK-19 实施修正 / 实施记录」。

## 00:10 TASK-19 完成：改动知情与可逆（P1 数据安全红线）

TASK-11 按用户指示顺延，先做 TASK-19（依据 `DECISION_LOG.md` ADR-15）。

### 交付概要

| 部分 | 内容 |
|:---|:---|
| Part A 知情 | `src/components/LinkNoticeDialog.vue`（首次同步前一次性告知断链）+ `src/composables/useLinkNotice.ts`（闸门） |
| Part B 可逆 | `list_backups` / `restore_backup` 两条命令 + 设置页「备份与还原」区 + 二次确认弹窗 |
| 共享层 | `Adapter::l0_backup() -> Option<L0Backup>`（默认 `None`）+ `restore_l0_backup()` |
| 状态 | `AppState.link_notice_shown`（并入 `StartupState` 一次返回） |

验收：`cargo test` **118 passed / 0 failed / 0 warning**、`dryrun_sync` **11 节 0 项失败**、
`pnpm build` 零报错、真实数据零改动。

### ⭐ 本次形成的新约定（跨会话有效）

**1. 两个快照后缀，语义互不相同，别混用**

| 后缀 | 内容 | 可覆盖性 |
|:---|:---|:---|
| `.crossbrain-backup` | 首次注入前的**原始文件** | **任何代码路径都不得覆盖** |
| `.crossbrain-before-restore` | **最近一次还原前的现场** | 滚动；**仅当现场 ≠ 备份内容时**才写 |

第二条里的「仅当不同」不是优化而是正确性要求：无条件写会让连续第二次还原用
「已还原后的内容」覆盖掉第一次留下的**真实现场**，为「可逆」而做的功能反而造成不可逆丢失。

**2. `Adapter` 新增可选方法 `l0_backup()` —— 默认返回 `None`（第 4 个差异项）**

```rust
fn l0_backup(&self) -> Option<L0Backup> {
    Some(L0Backup { target_file: …, backup_file: … })   // 只有标记块注入型需要覆盖
}
```

返回**一对**路径而非单个备份路径：还原同时需要落点与快照，只给备份路径就得到
「把 `.crossbrain-backup` 去掉反推文件名」——又是一份会分叉的命名规则副本。
给默认实现后，Antigravity 与全部测试 Mock 一行都不用改。

**3. 前端：新增 `src/composables/`（本项目首个组合式函数）**

`useLinkNotice(initiallyShown)`。**里面有一条不能写错的顺序约束：标记「已展示」必须写在
`confirm()` 内（用户确认之后），不能写在 `guard()`（弹窗那一刻）**——
否则用户点取消或关窗口后，这条告知永远不会再出现。两条同步入口（向导步骤 4 /
主工作台「立即同步」）必须共用它，各写一份迟早在一侧写错。

**4. 新增了一条「反向验证过的」契约测试**

`tests/ipc_contract.rs::link_notice_is_marked_only_after_confirm` 用源码文本锁定第 3 条约束。
**写完后特意反向验证过**：把 `markLinkNoticeShown()` 挪进 `guard()` → 测试如期失败。
> 这条习惯值得保持：纯文本断言极易写成「永远为真」，不反向跑一次等于没测。

同时补了 `restore_backup_argument_name_matches_on_both_sides` —— 命令**参数名**
（前端 `toolId` ↔ Rust `tool_id`）同样是字符串寻址，写错的表现是「按钮点了没反应」，
而 `cargo test` 与 `pnpm build` 双双通过。

**5. 术语检查扩围**

`wizard_copy_has_no_internal_terms` 原本只扫 `WizardView.vue` → 新页面会成绕过口。
已抽 `template_of()` 改为扫三个模板：`WizardView.vue` / `MainView.vue` / `LinkNoticeDialog.vue`。
**以后每新增面向用户的界面文件，都要把它加进这个列表。**

**6. 已知坑复述（本次又踩到一次预防）**

新增 Naive UI 组件（本次是 `NModal` / `NTag`）后，必须**先 `npx vite build`**
（重新生成 `src/components.d.ts`）再 `pnpm build`，否则 `vue-tsc` 报找不到组件类型。

### ⚠️ 遗留（3 项，均不阻塞）

1. **GUI 未做点击穿过验证**。已完成 `cargo build` + 118 单测 + 干跑 11 节 + `vue-tsc`
   + 产物内容核对（新文案与新命令名都在 `dist/assets/*.js` 里），但**没真的开窗口点一遍**
   「首次同步弹说明 → 设置页还原」。没开的原因：拉起 `pnpm tauri dev` 会在用户机器上
   弹出他未要求的窗口。**建议由用户跑一次目视确认，或并入 TASK-15 端到端测试。**
2. **还原后不自动重新同步**，界面上也未提示「想继续用请再点一次立即同步」。刻意不自动
   （用户按还原就是要回到最初），但文案待补——可并入 TASK-13 / TASK-14。
3. `*.crossbrain-before-restore` **无清理入口**，每次还原会更新一份，目前只能手工删。
   与「备份清理」一起作为后续可选优化。

### 下一步

`TASK-11`：全局规则编辑器工作台（Markdown 编辑 + 实时预览）。

---

## 00:20 版本控制接入：本地仓库初始化 + 首推 GitHub

用户新建了远端仓库 `git@github.com:JasonOracle/CrossBrain.git`，并提出工作约定
**「每做完一个 task 就 git 提交一次」**。

### 做了什么

| 项 | 内容 |
|:---|:---|
| 仓库初始化 | `git init -b main`（此前本目录**不是** git 仓库） |
| `.gitattributes` | **新建**。`* text=auto eol=lf` + 图片/字体 binary + 锁文件 `linguist-generated` |
| `.gitignore` | 在 Vite 默认模板上补 `src-tauri/target/`、`src-tauri/gen/schemas/`、`*.pdb`、`Thumbs.db` 等 |
| `AGENTS.md` | **新增「🔀 版本控制」章节**（远端地址、提交粒度、提交信息格式、提交前先跑安全回归），并把安全红线补第 5 条「绝不提交构建产物与密钥」 |
| 首个提交 | `5ad89d8` — 90 files / 25302 insertions |
| 推送 | `git push -u origin main` 成功，远端 `refs/heads/main = 5ad89d8`（直连 `ls-remote` 已核） |

`.workbuddy/`（记忆 + 项目技能）经用户确认**全纳入**版本控制。

### ⭐ 本次形成的约定

1. **换行符统一为 LF**（`.gitattributes`）。本机 `core.autocrlf=true`，不做归一化的话
   每次 `git add` 都刷一屏 `LF will be replaced by CRLF` 警告，且跨机器协作会产生
   整文件级 diff。已确认仓库内**没有** `.bat` / `.cmd` / `.ps1`（这三类才需要 CRLF），
   所以 `eol=lf` 是安全的。**以后新增此类脚本要单独豁免。**
2. **提交粒度 = 一个 task 一次**，提交信息格式：
   `<type>: TASK-XX <一句话>` + 正文（做了什么/为什么/坑）+ `验证：…` 一行。
3. **9.2 GB 的 `src-tauri/target/` 是最大风险点**。`src-tauri/.gitignore` 里的 `/target/`
   已生效（`git check-ignore` 实测），根 `.gitignore` 又兜了一层。提交前用
   `git diff --cached --name-only | wc -l` 看暂存文件数——正常应在 **90 上下**，
   一旦变成几千就说明忽略规则失效了。
4. `AGENTS.md` 里那句「代码尚未开始，等待 TASK-01」**早已过期**（实际已到 TASK-19）。
   已改成**指向 `CURRENT_STATUS.md` 的指针**，并写明「此处不要再复制进度描述」——
   同样的复制粘贴已经在 `CURRENT_STATUS.md` 与 `AGENTS.md` 之间分叉过一次。
5. 本机 git 身份已是全局配置：`jasonoracle` / `liuyn2017@qq.com`。
   SSH 方式推送**无需交互**，`GIT_TERMINAL_PROMPT=0` 下直接成功。

### 关于「沙箱里 git 远端引用不落盘」

用户级记忆里记着 2026-09-14 的坑（push 成功但 `refs/remotes/origin/*` 为空、
`git status` 永远显示 upstream gone）。**本次未复现**：push 后 `git status -sb`
正常显示 `## main...origin/main`，无 ahead/behind 漂移。该坑应属**偶发/环境相关**，
已在使用者级记忆里补注，下次遇到先重试、别急着当成配置问题排查。

---

## 00:55 TASK-11 完成：全局规则编辑器工作台（左右分栏 + 实时预览）

详细交付与 10 条实施修正见 `docs/impl/TASK_BREAKDOWN.md` 的 TASK-11 两节；此处只记
「文档里没有、下次必须知道」的部分。

### ⭐ 新增的跨会话约定 / 坑

1. **Markdown 预览是安全边界，不是排版问题**。`tauri.conf.json` 的 `csp` 是 `null`，
   webview 里的脚本有 `invoke` 权限——预览若渲染原始 HTML，等于把本地文件读写权交给
   任意脚本。markdown-it 默认 `html:false` 转义原始 HTML（实测 `<b>` → `&lt;b&gt;`），
   因此**严禁打开 html 开关**（`markdown_preview_must_escape_raw_html` 锁死）。
   选 markdown-it 而非 marked+dompurify：单依赖且默认安全。
2. **预览区链接必须拦截点击**（`@click.capture` + preventDefault），否则 webview 导航
   会把应用带离界面。v-html 内容不受 scoped 样式影响 → `.md-preview` 必须放全局 `style.css`。
3. **`n-tab-pane` 默认 `display-directive="if"` 会卸载面板**，编辑类内容（未保存修改）
   跟着丢 → 有可编辑状态的 Tab 一律 `show:lazy`。
4. **`n-message-provider` 必须包在所有视图外层**（App.vue）。放进 `v-if` 分支，
   另一分支的 `useMessage()` 运行时才报错，构建期看不见。
5. **「未保存」按内容基线判断**（`dirty = content !== baseline`），比「敲过键盘」准确：
   改了又改回原样不算未保存，且天然满足向导「没动过不写盘」。
6. **「读不出来 ≠ 空」同样约束编辑器**：baseline 未建立前禁用保存，
   否则用户会用空文件覆盖自己的规则（与 ADR-14 同源）。
7. **主工作台的模板入口只在内容为空时显示**：模板一点就整篇替换，在有内容时显示 =
   静默覆盖用户规则。用「空白才显示」从机制上消灭，而不是靠确认弹窗。
8. 分栏编辑框用**原生 `<textarea>`** 而非 `n-input`：分栏需要固定高度 + 各自滚动，
   `n-input` 的内部高度机制难控，原生 + Tailwind 完全可控。

### 🔥 大发现：用户 00:18 真机同步了（首份真实环境验证数据）

用户真的跑了一次应用并同步。真实数据全部符合设计：
- **断链只断 CLAUDE.md 侧**：CLAUDE.md links=5 → **1**；其余 4 路仍同一 inode（links=4、
  md5 全等 `47645f60…`）。D-03 实测正确。
- CLAUDE.md 用户原文 1908 字节完整保留，`.crossbrain-backup` 仍是原始内容（可还原）。
- Codex `AGENTS.md` 0 → 579 字节（首注）；L0 `crossbrain-L0.md` 444 字节写入。
- **`crossbrain-spike-test` 遗留孤儿被真机同步按设计删除**（验收样本已消费）。
- 顺带发现：用户 CLAUDE.md 第 39 行手写了一行带中文逗号的 `@~/.ai-profile/AGENTS.md，`
  （在标记块外）——与标记块内的引用重复且可能解析失败。已提醒用户自行清理，
  CrossBrain 不代改标记块外的内容。

**因此安全回归基线已变**（硬链接组 4+1、快照 2949 项），MEMORY.md 已更新——
下次会话别再拿 links=5 当「正常」。

### 连带修复（同一轮发现）

`dryrun_sync` 第 [6]/[11] 节断言假设「真实 CLAUDE.md 从未同步过」，被上述真机同步
直接推翻 → 2 项假失败。已改为**预状态自适应**（跑前探测标记块与备份，走首注或更新分支）。
教训：**干跑的断言不要建立在「用户的机器处于某个状态」上**，先探测再分支。
顺带修了 `copy_codex_subset` 不复制 Codex 备份文件的保真度缺口。

### 验证（2026-09-15 00:55）

`pnpm build` 零报错（产物 477 KB，+115 KB 来自 markdown-it）；`cargo test`
**120 passed / 0 failed / 0 warning**；干跑 **0 项未通过**；新增 3 条契约测试全部
**反向验证过**（故意塞入 `html:true` / `runSync` / `slug`，三条如期失败后还原）。

### 遗留

① GUI 点击走查未做（与 TASK-19 的遗留合并，建议用户跑 `pnpm tauri dev` 或并入 TASK-15）；
② `csp: null` 建议独立小任务收紧（纵深防御）；③ 切 Tab 不拦截未保存修改；
④ `markdown-it` 使产物 +115 KB，如在意可评估按需加载。
